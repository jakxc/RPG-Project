namespace RPG.Control
{
    // The different types of cursors for different interactions (cursor affordance)
    public enum CursorType
    {
        None,
        Movement,
        Combat,
        UI,
        Pickup
    }
}